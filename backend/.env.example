# Backend Environment Variables Example

# Server Configuration
PORT=8000
ENVIRONMENT=development
ALLOWED_ORIGINS=http://localhost:5173,https://sentryfi.vercel.app

# Note: Add any cloud service keys (e.g. OCR API keys) if needed in the future
